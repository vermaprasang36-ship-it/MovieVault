# MovieVault
MovieVault is a Python + SQL movie discovery app built with Streamlit. It lets users search movies using the TMDB API, view movie details, filter by genre/rating, and save selected movies to a personal watchlist stored in SQLite.
